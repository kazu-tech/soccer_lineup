.wrapper
  = render "header"
.main
  .game_box
    - @posts.each do |post|
      .data-box
        .game-date
          = post.date
        .game-match
          = link_to post.match, post_path(post.id)
        .game-result
          = post.result
        .game-text
          = post.text